# Legacy Artifacts (ReasonKit Core)

This directory contains archived code from earlier development iterations.

- `legacy/arf-platform/` — ARF Rust workspace (archived)
- `legacy/rust-core/` — Legacy Rust core with Python bindings (archived)
